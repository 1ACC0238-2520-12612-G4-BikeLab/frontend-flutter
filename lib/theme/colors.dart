import 'package:flutter/material.dart';

class BikeLabColors {
  // Verde lima brillante (sidebar, botones, acentos)
  static const Color limeGreen = Color(0xFF9CEE48);
  
  // Verde más oscuro/muted (para "LAB" en el título)
  static const Color mutedGreen = Color(0xFF2ECC71);
  
  // Negro (fondo principal)
  static const Color black = Color(0xFF000000);
  
  // Blanco (texto)
  static const Color white = Color(0xFFFFFFFF);
  
  // Naranja (para el logo, si se necesita)
  static const Color orange = Color(0xFFFF6B35);
  
  // Rojo (para detalles del logo)
  static const Color red = Color(0xFFFF0000);
}

